#include <iostream>
using namespace std;

int main() {
    int a, b;
    int max , min ;

    // Nhập hai số
    cout << "Nhap so thu nhat: ";
    cin >> a;
    cout << "Nhap so thu hai: ";
    cin >> b;

    // Tìm số lớn hơn và nhỏ hơn bằng toán tử điều kiện
     if (max = (a > b) ){
     	cout << " day la Max ";
     	
	 }
	 else if (min = (a < b)) {
	 	cout << " Day la Min ";
	
	 }

    // In kết quả
    //cout << "So lon hon (Max) la: " << max << endl;
    //cout << "So nho hon (Min) la: " << min << endl;

    return 0;
}
